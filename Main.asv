Image = imread('C:\Users\DELL\Desktop\image processing\Lab 7\Task\morph.png');
Image = rgb2gray(Image);
Image = imbinarize(Image);
complement = ~Image
Fraction = WhiteCount(Image);
Fraction

%%%%%%%
se = strel('disk', 4);
close = imdilate(Image, se);

figure,imshow(close);

